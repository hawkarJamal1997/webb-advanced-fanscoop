<div class="content">
    <h2> Create blog post about {{club}} </h2>
    <p>Create a new blog post for {{club}} using the form below.</p>
    <div class="container">
        <div class="columns is-centered"> 
            <div class="column is-7-desktop is-10-mobile is-5-widescreen">
                <form action="/posts/create/{{club}}" method="POST" class="box">
                    {{#if errors.length}}
                        <ul>
                            {{#each errors}}
                            <li id="error-text">{{this}}</li>
                            {{/each}}
                        </ul>
                    {{/if}}
                    <div class="field">
                        <label class="label">Title:</label>
                        <input type="text" class="input" name="title" value="{{title}}">
                    </div>
                    <div class="field">
                        <label class="label">Content:</label>
                        <textarea class="textarea" name="content" rows="5">{{content}}</textarea>
                    </div>
                    <input type="hidden" name="club" value="{{club}}"
                    <div>
                        {{#if isLoggedIn}}
                            <input class="button is-success" type="submit" value="Create post">
                        {{else}}
                            <p class="subtitle" id="error-text">You need to sign in to create a post!</p>
                        {{/if}}

                    </div>
                </form>
            </div>
        </div>
    </div>
</div>